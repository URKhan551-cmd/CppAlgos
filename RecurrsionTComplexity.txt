TWO WAYS T FIND THE TC OF RECURSIVE CODE OR FUNCTION:

1: RECURRENCE RELATION  A METHEMATICAL APPROACH 
2: TOTAL NUMBER OF CALLS  x  WORK IN EACH CALL.


example::  int factorial(n){
   if(n==0){
   return 1;
   }

  return n * factotail(n-1);
};

recursive tree ::  if we get n=4  >>   f(4)   -> f(3)  ->  f(2) -> f(1)  -> f(0)
                                    24 return   <-   6*4  <-  2*3return    <-   1*2return    <-  1*1 return    <- 1 return
       here total number of calls are 5  if i have n into function i need to call n+1. or n times


         in each function call the work has been done by func is Constant    O(n * K);
O(n);


/////////


SPACE COMPLEXITY:
recursion call stack has been count on memory like a box holding each call of recursive functoin.

stack = f(4) + f(3) + f(2) + f(1) + f(0)   it occupy space in memory;

AUXILARY SPCAE = variable we create to hold values.
+
CALL STACK 


formula:  depth of recusion tree  x   memory in each call    
OR
height of call stack  x  memoy in each call.

height like => 4-> 3-> 2-> 1-> 0;
memory in each call is constant 

so over all SpaceComplexity  = n is heigt x K constnat

n x K =    O(n) 



RECURSIVE FIBONACCI:: TIME &  SPACE Complexity?
int fib(int n){
  if(n==0 || n==1){ return n; };

return fib(n-1) + fib(n-2);
};

f(n) =  f(n-1) + f(n-2);
let us say we get n=4

f(4) = f(4-1) + f(4-2)   
        f(3) = f(3-1)+f(3-2);      f(2)=f(2-1)+ f(2-2);
          f(2)= f(2-1)+f(2-2);  f(1)=f(1-1)+f(1-2);stop        f(1)=f(1-1)+f(1-2)stop  f(0)=f(0-1)+f(0-2)stop
              f(1)=f(1-1)+f(1-2)stop;

here we can see a tree like structure  every functio has been calling twice to its function value;
every function has been calls twice with different value of n

where the base case hit stop calls.
f(4) ->       f(3) & ->                 f(2)
             f(2) & ->   f(1)hit             f(1)hit & f(0)hit
         f(1)hit & f(0)hit

TC = total num of calls  * workin each call

TC = 1+2+4+8  *  total work in each call is Constant K
total number of calls 2^n-1 . if have n=4   total calls will be 2^4-1 = 2^3 = 8....

TC = 2^n-1 * K   =   remove K & 1 constant
TC = 2^n =  O(2^n);


SPACE COMPLEXITY =  DEPTH OR HEIGT OF RECURSIVE CALLS  X  MEMORY
n x total work at each IS CONSTANT K

n x k = O(n)

SPACE COMPLEXITY = O(n);
//////////////////////////////////////////////////////////
HOW DO I FIND THE RECURRENCE RELATION TO TC;

So if T(n) is the running time,

the current call spends:

T(n-1) solving the first problem
T(n-2) solving the second problem
Θ(1) for the current work


T(n) = T(n-1) + T(n-2) + O(1) the last one is for current work which is constant

fib(5)

                 fib(5)
               /        \
          fib(4)        fib(3)
         /     \       /     \
    fib(3) fib(2) fib(2) fib(1)
     /  \    / \    / \
fib(2)fib(1)...

Notice something?

fib(3) is computed multiple times.

fib(2) is computed even more times.

The amount of work grows exponentially.

2^n   called exponential growth.


///////////////////////////

MERGE SORT ALGORITHM TIME COMPLEXITY.

void mergeSort(int arr[], int st, int end){
       if(st >= end){
return;
       };

int mid = st + (end - st)/2;

mergeSort(arr, st, mid);     here call
mergeSort(arr, mid+1, end);   here call

merge(arr, st, mid, end);    merge is a funtion whichis outside of mergeSort but we have been calling it inside of mergeSOrt.

   }


TC = total call *  workdone in each call  --->> merge functiona
Tc = total call *  workdone in each call -->> mergeSort recurse function 

then COMBINE both TC of those Function.

merge function  normal function here 4 loop has been going there.

merge function has been doing to merge two array loop has been performing ths which give us 3loop O(n);
and last loop it self O(n);

MERGE has total TC = O(n + n) = O(2n)   
we ignore the constant = =  O(n);


now MERGE SORT:  do the same as half the value in each call
i have an arr size = 4

first call 4   into  2  &  2
second 2 call  1  &  1    &  2nd call   - 2 & 2

then Backtrack again 1+1   2+2  

each call f(4) ->> f(n/2) & f(n/2)  
             f(n/4) & f(n/4)     f(n/4) & f(n/4)

it means  each call make n ->  n/2 ->  n/4   n/8  .... 1
OR  we can say    n/2^0  ->>  n/2^1  ->> n/2^2  -->> n/2^3 .... n/2^k

if i have x level   then n/2^x      n/2^x = 1   ->>    n=2^x      -->>  logn = x

levels = x = logn   

TC = total calls * work done

merge FUNCTon And Merge SOrt give us

O(n) * O(log n) =  O(n log n);

OUR FINAL ANSWER   O( n log n );



SPACE COMPLEXITY = each level n space occupy   then release give it to the second stack function holds n memory.

O(n);
