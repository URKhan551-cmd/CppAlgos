TIME COMPLEXITY :: not an actual time taken by whole progrm but amount of time taken as function of input size (n);

Common time complexities.

O(1) : conatant :: 
                  like  i have a function which calculate the sum of 1 to n
                  function int n;
                   cin>> n;
                   int ans = n * (n+1)/2;
                                         here the time complexity is O(1) constant.


or  if we found arr[0] position element bynhelp of funcion we can directly access them in one operation so its constant.
and i have sorted arr ascending order it mrean smaller value are at the start and bigger values are at the end.
so we can accces bigger value of that particular arr in one operation O(1)= constant.

if there is no loop & if considyion Number of operation count the complexity.


BIG OF N  O(n):: 
               int fact = 1;
               for(int i=0; i< n; i++){   here the loop run n times where it perform conatant operation per run  
              fact *= i;                  so the O(n * k) where we ignore k so O(n);
               }

Nth Fibonacci-DP
      for(int i= 2; i<n; i++){
       dp[i] = dp[i-1] + dp[i-2]    here also the same n time loop run operation takes contant time 
      }                             O(n * k) = k ignore  O(n);


kadans ALgorithm ::
                   intt currSum = 0,  ans= INT_MIN;
              for(int i=0; i<n; i++){
               currSum += arr[i];
               ans = max(currSum, ans);
              currSum = currSum < 0 ? 0 : currSum;     n times loop run and perform k time operation;
              }                                        so the overall time O(n);


O(n2)  & O(n3)::

      BUBBLE SORT: if i have two nested loop first loop run n time and same as like for each element of the first loop 
                     how many times the second inner loop runs so n times 
                    first loop run   =  3 times * K operation perform 
                   second loop run =  2 times * k operation perfomr 
                    if condition run  = 1 * k operation 
 
                      get the total value of all the n (n-1)k + (n-2)k + (n-3)k ...... 2k * k;
                     k common  k [(n-1) + (n-2) + (n-3) ...];
                    O(n2); for bubbble sort


O(n3)::  where we see 3 nested loop 
         with k number of operation has been going on inside of it.
      first loop n time 
       second loop n time 
        third loop n times run.     over all time complexity will be O(n3);



O(log n): best OPTIMIZED 
log of n is a small value it lies after O(1) becuse its faster as comapre to O(n) but slower than contant time Complexity

           2^power here the power is LOG
BINARY SEARCH AND binary search tress (BST);

ints=0, end=n-1;
while (s<end){
int mid= s + (end - s/2);
if(arr[mid] < target){
s = mid + 1;

} else if(arr[mid] > target) {
  e = mid - 1;
}else {
return mid;     // ans 
}
};

        n at each step gonna divide by 2 until reach to 1
    n/2 =>> n/4   ==>> n/8 ..... n = 1
  n/2^0  ==>> n/2^1 ==>> n/2^2   ==>> n/2^3 ...... n/2^x = 1    here we wanna find x value.



//////////////////////////////////////////////////////////////

O(n log n):   sorting  / merge sort / quick sort / greedy algo.

O(n log n)  better than O(n2)
O(n log n)  worse then O(n);


//////////////////////////////////////////////////

O(2^n) EXponential TC
the worst complexity comes in recurrsion.
      here it mean 2^n    = 2x2x2x2x2x2x2x2

///////////////////////////////
O(n!)  factorial complexity
also in recurrsion  n queens / knights tour / string All possible permuattion.

                                                       string like "ABC"
                                                     how many possible permuattion of this string.
                                                      abc, acb, bac,bca, cab, cba, total 6;
                                                    6 = 3! factorial    = n! factorial.
n! = nx (n-1) x (n-2) x (n-3) ...



EXAMPLE:: 
    PRIME NUMBER:
        for(int i=2; i*i<=n; i++){       // here the loop runs for i=2  i^2 <= n
           if(n % i ==0){               //  i2= n   ==>>  i=root of n  
             cout<< "NON Prime"<<endl;  //  i=2  to i root n   or 0 to root n
             break;                      // O(root n) > O(n)
           }
        };


//////////////////////


which one is better TC    O(root n)  or O(n) 

O(n)       = 100 operations
O(√n)      = 10 operations.


which one is better TC    O(root n) or O(log n)

O(n) vs O(logn)

 Answer: O(logn) is better.

n = 1,000,000             Square root:   √1,000,000 = 1000

Log base 2:            log₂(1,000,000) ≈ 20

Compare:

O(√n)   = 1000 operations
O(log n)=   20 operations




SELECTION SORT:: O(n2)
for(int i=0; i<n-1; i++){        // first loop   n times run 
 int minIndex = i;
for(int j = i+1; j<n; j++){     // second loop   n  j=1 to n      n time 
if(ar[j] < arr[minIndex]){                          j=2 to n      n-1 time
minIndex = j;                                       j=3 to n      n-2 time
}
}
swap(arr[i], arr[minIndex])
}

the inner loop runs total   n*(n + 1)/2   times

///////////////////////////////////

RECURRSION TIME COMPLEXITY:

2 WAYS to find recurrsion tc.

1st is Recurrsion Relation   mathematicaly  
RECURRENCE : each call does constant work and each call.
T(n) = T(n-1) + O(1)

t(n) = t(n-1) + 1
t(n) = t(n-2) + 1 + 1
t(n) = t(n-3) + 1 + 1 + 1
t(0) + n   = O(n) time complexity 

2nd    how many times the function calls * how many operation call performs inside of it.
      if n =4   there will be 5 calls : 
   for n value we call n+1 calss;

n is number of call *  K      O(n)

EXAMPLE:
for this problem  int factorial(int n){
if(n == 0){
return 1;
};
return n * factorial(n-1);
}


// Method to find complexity of recurrsion prob
DRAW A RECURRSION TREE.

int fib(int n){
    if(n <= 1)
        return n;

    return fib(n-1) + fib(n-2);
}

               fib(5)
             /        \
        fib(4)       fib(3)
       /     \       /    \
   fib(3) fib(2) fib(2) fib(1)

here you can see there are many double calls. fib(3)  and fib(2)

The number of calls roughly doubles each level.

O(2^n);


/////////////////////////
STANDARD INTERVIEW METHOD::
This is the standard interview method.

int binarySearch(...)
{
    ...
    return binarySearch(...);
}

Each call searches only half the array.    n/2 half of the arr at each call

RECURRENCE  T(n) = T(n/2) + 1
for   t(n)  =  T(n/2) + 1;
             =  T(n/4) + 2;
              = T(n/8) + 3;
              = T(n/16) + 4;

when does it stop  (  n/2^k = 1  );  ==>>  2^k = n

k =log2 n
O(log n);  TC
///////////////////////////////////////////////

overview :  if at recursion call the main function has been call over and over but with a value has been decreasing by -1 
it mean every call has been done with just 1 difference  here the TC will be O(n);


if a call has been happen with half of the value to first call.
like at each call the value of function call has beeen divide by 2. then it will be TC =O(log n)


outside of the recurrsion something happen is consider as contant TC  O(1);

1 how many recurse call happen 
2 how much the input got decrease
3 outside of recurse call what ever you assigned value will be constant
//////////////////////////////////

Master level of TC for HEAVY ALGORITHMS.







